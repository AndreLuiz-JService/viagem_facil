import 'package:flutter/material.dart';

class ListPublicacao extends StatelessWidget {
  const ListPublicacao({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}